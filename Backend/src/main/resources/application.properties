# Server configuration - Render utilise une variable PORT dynamique
server.port=${PORT:8080}

# Application name
spring.application.name=qcm-generator

# Logging
logging.level.com.qcm.generator=INFO
logging.level.org.springframework.web=INFO

# OpenAI Configuration
openai.api.key=${OPENAI_API_KEY}